/** @LoginContext.js
 *  This component is a wrapper which will provide
 *  the LoginContext Context to other needy components
 *  */

import { useState } from "react";

import { FetchWrapper } from "../utils/api.service";
import { LOGIN_URLS } from "../utils/api_resource";

const LoginContext = () => {
  return <>kush</>;
};

export default LoginContext;
